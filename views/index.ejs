<% if (query.logoutSuccess) { %>
  <p class="success-message">You have been successfully logged out.</p>
<% } %>

  <!-- Quote Modal -->
  <!-- <div id="quoteModal" class="modal">
    <div class="modal-content">
        <p id="quoteText"></p>
        <p id="quoteAuthor"></p>
    </div>
  </div> -->

<div class="author">
  <h1 class="author__heading">Hi, I'm David</h1>
  <p class="author__body">Web developer and content creator.</p>
</div>

<img src="/img/blur_edges.png" alt="It's me!!" class="hero-image">



<section class="articles">
  <h2 class="articles__heading">Latest Posts</h2>

  <ul class="article-ul">
    <% data.forEach(post => { %>
      <li>
        <a href="/post/<%= post._id %>">
          <span><%= post.title %></span>
          <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
        </a>
      </li>
    <% }) %>
  </ul>


  <% if (nextPage !== null) { %>
    <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Posts</a>
  <% } %>
 

  
  



</section>

